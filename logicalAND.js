//Create a mystery variable and a conditional for it which will print "YOU GOT IT!!!" only if the first letter is 'P', the length is 5+ characters and has the number 7

const mystery = ''; //CHANGE THIS VALUE TO MAKE THE CONDITIONAL BELOW TRUE
if(mystery[0] === 'P' && mystery.length > 5 && mystery.indexOf('7') !== -1){
    console.log("YOU GOT IT!!!");
}
